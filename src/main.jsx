import React from 'react'
import ReactDOM from 'react-dom/client'
import GiftFinder from '../gift-finder.tsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <GiftFinder />
  </React.StrictMode>
)
